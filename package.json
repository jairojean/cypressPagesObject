{
  "name": "gerenciador_gastos_cypress",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "cy:open:chrome": "npx cypress open --browser chrome",
    "cy:run:chrome": "npx cypress run --browser chrome",
    "cypress:open:dev": "npx cypress open --config-file config-dev.js  --browser chrome",
    "cypress:open:qa": "npx cypress open --config-file config-qa.js  --browser chrome"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^14.5.4",
    "cypress-mochawesome-reporter": "^4.0.2",
    "cypress-multi-reporters": "^2.0.5",
    "dotenv": "^17.2.3",
    "mocha-junit-reporter": "^2.2.1"
  }
}
